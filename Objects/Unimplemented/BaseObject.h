// BaseObject defines an object all non-core objects inherit from
// Anything that can be rendered in the heirachy display window must be a BaseObject

// BaseObject has:
// * Name -- it's name in the heirachy
// * Archivable -- whether it can be saved or will be ignored
// * Children -- which BaseObjects are parented to this BaseObject (by id)
// * UniqueId -- an ID that uniquely defines the specific object
// * Icon -- an icon to be displayed by the Heirachy tab

#pragma once